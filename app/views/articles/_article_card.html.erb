<%= link_to article_path(article) do %>
  <div class="card-article">
    <%= image_tag article.banner_url %>
    <div class="card-article-infos">
      <div>
        <h2><%= article.name %></h2>
        <p><%= article.description %></p>
      </div>
    </div>
  </div>
<% end %>
